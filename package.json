{
  "name": "crestbourne-portfolio",
  "version": "1.0.0",
  "description": "Crestbourne Property Investment Portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build required'",
    "deploy": "vercel --prod",
    "migrate": "node database/migrate-all-portfolios.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
